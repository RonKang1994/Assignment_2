# create your Kivy layout in the kv language here
BoxLayout:
    orientation: 'horizontal'
    BoxLayout:
        orientation: 'vertical'
        Label:
            text: 'Sort by:'
        Button:
            text: 'Year'
            id: sort_label
            on_press: app.handle_sort()
        Label:
            text: 'Add New Song....'
        Label:
            text: 'Title:'
        TextInput:
            id: input_title
            text: ''
            # Centralize it
            padding: [10, (self.height-self.line_height)/2]
        Label:
            text: 'Artist:'
        TextInput:
            id: input_title
            text: ''
            # Centralize it
            padding: [10, (self.height-self.line_height)/2]
        Label:
            text: 'Year:'
        TextInput:
            id: input_title
            text: ''
            # Centralize it
            padding: [10, (self.height-self.line_height)/2]
        Button:
            text: 'Add Song'
            #on_press: app.handle_add_song()
            on_press: app.add_song_button()
        Button:
            text: 'Clear'
            on_press: app.handle_clear()
    BoxLayout:
        orientation: 'vertical'
        Label:
            text: 'Test'
        Button:
            id: songs
            cols: 1